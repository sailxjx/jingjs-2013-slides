/*
// turn on to populate the first time:
var placename = require('placename');
placename.db.batch(require('./data.json').map(function (row) {
    return { type: 'put', key: row.key, value: row.value };
}));
*/
